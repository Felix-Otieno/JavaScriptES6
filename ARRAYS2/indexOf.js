// It returns the index of the first occurrence of a specified element in the array. If the element is not found, it returns -1.
let fruits = ['Apple', 'Banana', 'Orange'];
let index = fruits.indexOf('Banana'); // Returns 1
console.log(index);