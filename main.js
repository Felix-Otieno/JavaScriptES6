"use strict"; //When you use strict mode if the variable is not defined by the keyword it will not display the output, thus it will bre an exception saying referenceError.
let car = "Toyota";
Car = "Nissan";
console.log(car);